{
	"name": "echoes-of-deep",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "node generate-content.js && vite dev",
		"build": "node generate-content.js && vite build",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"generate-content": "node generate-content.js"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^6.0.0",
		"@sveltejs/kit": "^2.22.0",
		"@sveltejs/vite-plugin-svelte": "^6.0.0",
		"glob": "^11.0.3",
		"svelte": "^5.0.0",
		"svelte-check": "^4.0.0",
		"typescript": "^5.0.0",
		"vite": "^7.0.4"
	},
	"dependencies": {
		"gray-matter": "^4.0.3",
		"marked": "^16.1.2"
	}
}
